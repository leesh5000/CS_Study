    # def DFS(r, c):
    #     global res
    #     res[r][c] = addr
    #     for i in range(4):
    #         if 0 <= r+dr[i] <= n-1 and 0 <= c+dc[i] <= n-1 and board[r+dr[i]][c+dc[i]]:
    #             DFS(r+dr[i], c+dc[i])